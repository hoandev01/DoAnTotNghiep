@model ChickenF.Models.AdminDashboardViewModel

<h2 class="text-3xl font-semibold mb-6 text-gray-900">Quản lý Nhân viên</h2>
<div class="overflow-x-auto">
    <table class="w-full border-collapse rounded-xl shadow-md divide-y divide-gray-200">
        <thead class="bg-gray-50">
            <tr>
                <th class="text-left px-6 py-3 font-semibold text-gray-700">Tên</th>
                <th class="text-left px-6 py-3 font-semibold text-gray-700">Tên đăng nhập</th>
                <th class="text-left px-6 py-3 font-semibold text-gray-700">Email</th>
                <th class="text-left px-6 py-3 font-semibold text-gray-700">Số điện thoại</th>
                <th class="text-left px-6 py-3 font-semibold text-gray-700">Hành động</th>
            </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
            @foreach (var emp in Model.Employees)
            {
                <tr class="hover:bg-gray-50 transition duration-200">
                    <td class="px-6 py-4">@emp.FullName</td>
                    <td class="px-6 py-4">@emp.Username</td>
                    <td class="px-6 py-4">@emp.Email</td>
                    <td class="px-6 py-4">@emp.Phone</td>
                    <td class="px-6 py-4 space-x-4">
                        <a asp-controller="Employee" asp-action="Edit" asp-route-id="@emp.Id" class="text-indigo-600 font-semibold hover:underline">Sửa</a>
                        <a asp-controller="Employee" asp-action="Details" asp-route-id="@emp.Id" class="text-gray-600 font-semibold hover:underline">Chi tiết</a>
                        <a asp-controller="Employee" asp-action="Delete" asp-route-id="@emp.Id" class="text-red-600 font-semibold hover:underline">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>